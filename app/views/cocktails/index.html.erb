<% @cocktails.each do |cocktail| %>

<h2 class="text-center"><%= link_to cocktail.name, cocktail_path(cocktail) %></h2>

<% end %>

<h1 class="text-center"> <%= link_to "Add a cocktail", new_cocktail_path, class: "btn btn-success btn-md" %></h1>
