
<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= simple_form_for [cocktail, dose] do |f| %>
        <%= f.error_notification %>
        <% if @dose.errors.any? %>
            <%= @dose.errors.full_messages.join(" ") %>
        <% end %>

        <%= f.input :ingredient_id, collection: ingredients %>
        <%= f.input :description %>
        <%= f.button :submit %>
      <% end %>
    </div>
  </div>
</div>
